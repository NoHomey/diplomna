#include <iostream>
#include <wiringPi.h>
using namespace std;

int main() {
  wiringPiSetup();
  pinMode(15, INPUT);
  pullUpDnControl(15, PUD_UP);
  pinMode(5, INPUT);
  pullUpDnControl(5, PUD_UP);
  return 0;
}

